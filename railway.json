{
  "build": {
    "builder": "shell",
    "buildCommand": "cd Back-End && ./mvnw package -DskipTests"
  },
  "deploy": {
    "startCommand": "cd Back-End && java -jar target/*.jar",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
